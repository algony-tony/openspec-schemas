# Project Scaffold: [Project Name]

## Repo Layout
[Directory structure + responsibilities.]

## Tooling
[Lint, format, typecheck, package manager.]

## Config & Environments (12-factor)
[Config strategy, env vars, secrets.]

## CI/CD
[Pipeline stages, gates.]

## Run & Entry
[The single command to run the whole app, the ONE entry URL a user opens, and
how frontend & backend connect (backend serves the built SPA? dev proxy? two
servers — which is the entry?). The backend root must not 404 for a user.]

## IaC & Local Dev
[Infra-as-code baseline, local setup commands.]
