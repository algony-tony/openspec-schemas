# Spec: [capability]

## ADDED Requirements

### Requirement: [name]
WHEN [trigger], the system SHALL [response].

#### Scenario: [name]
- **WHEN** [...]
- **THEN** [...]

### Requirement: [non-functional name]
The system SHALL [measurable target — e.g. p95 < 200ms].

#### Scenario: [name]
- **WHEN** [...]
- **THEN** [...]
